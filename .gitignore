__pycache__/
*.pyc
landing/
DATA_PROFILE.md
.ipynb_checkpoints/

# Trained by setup.sh, not committed: the candidate is ~95 MB, and an artefact
# you can rebuild in seconds does not belong in a repository students clone.
service/artefacts/
data/*.parquet
data/MANIFEST.json
out/
mlruns.db
mlartifacts/
mlruns/
labs/.your_attempt/
# Written by verify/_harness.py's explain(): how many times this student has
# failed each graded assertion, so the reasoning can be withheld until the third
# attempt. It is one student's state, not the repository's.
labs/.attempts.json
timing.json
